Code review

HW15
#1
*sql*
- используй code style как принято в SQL

*SqlStorage*
- раньше контакты удалялись правильно, в той же транзакции... ты зарефакторил и теперь
неправильно +++
- deleteSections, аналогично...  должно быть один метод = одна транзакция +++
- потестируй update так чтобы секции тоже менялись, сейчас реализация с ошибкой +++
- getAll не нужен +++
- getAllSorted, на больших объямах join на несколько таблиц использовать неэффективно,
свалишь БД (декартово произведение) +++
в задании не просто так про отдельные запросы было написано

*сервлет*
- не требовалось всю инфо выводить, только имя и uuid, но получилось
красиво :slightly_smiling_face: (edited) +++

#2
привет
- на БД надо перекладывать работу, когда это уместно
если 1000 резюме с 6 секциями и 6 контактами, сколько записей выдаст join
 а сколько три запроса?
""Сделать отображение *таблицы резюме* в сервлете" - а в таблице резюме у нас только
имя и uuid (edited)
- get/getAllSorted, тут логичнее новый PreparedStatement на каждый запрос делать,
а не перезаписывать и в transactionalExecute уже есть обработка исключения
(в апдейт/save же нормально сделано, сделай по аналогии) (edited)